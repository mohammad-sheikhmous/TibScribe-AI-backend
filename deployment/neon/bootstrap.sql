-- Run while connected to your Neon project's default database as its owner.
-- Alternatively create the two databases from Neon Console > Branches > Roles & Databases.
CREATE DATABASE tibscribe_laravel;
CREATE DATABASE tibscribe_ai;
